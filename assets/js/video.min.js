var video = document.getElementById("vid");
var audioText = document.getElementById("audioButton");
var videoText = document.getElementById("videoButton");

video.volume = 0.1;


function videoPlay() {
if (video.paused) {
    video.play();
    videoText.innerHTML = "pause video";
  } else {
    video.pause();
    videoText.innerHTML = "resume video";
  }
}

function audioPlay() {
 if (video.volume == 0) {
  video.volume = 0.1;
  audioText.innerHTML = "mute video";
} else {
  video.volume = 0;
  audioText.innerHTML = "unmute video";
}
}

var mainVideo = $('video#vid');
var mobileSrc = "https://files.catbox.moe/v0dgib.mp4";
var desktopSrc = "https://files.catbox.moe/pxh5r1.mp4";
 
if ($(window).width() < 980) {
     mainVideo.append("<source type='video/mp4' src='" + mobileSrc + "' />");
} else {
     mainVideo.append("<source type='video/mp4' src='" + desktopSrc + "' />");
}

/*
var mainVideo = $('#sourcevid');

if ($(window).width() < 1200) {
  mainVideo.append("<source type='video/mp4' loopback='true' src='https://files.catbox.moe/v0dgib.mp4' />");
} else {
  mainVideo.append("<source type='video/mp4' loopback='true' src='https://files.catbox.moe/pxh5r1.mp4'/>");
}

// Wait until sources are appended to call MediaElements.js
mainVideo.mediaelementplayer();
*/
